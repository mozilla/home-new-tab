import { {{pascalCase componentName}} as Component } from "."

import type { Meta, StoryObj } from "@storybook/react-vite"

const meta: Meta<typeof Component> = {
  title: "{{pascalCase componentMain}} / {{titleCase storyName}}",
  component: Component,
}

export default meta

export const {{pascalCase storyName}}: StoryObj<typeof Component> = {
  render: () => {
    return <Component />
  },
}