/**
 * {{pascalCase stateName}} types
 * ---------------------------------------------------------
 * This file defines the shared data model for this feature.
 *
 * If TypeScript is yelling at you after changing the data shape,
 * this is usually the first place to check.
 *
 * Pattern:
 * - Define the shape of shared data here
 * - Define the actions that can update that data
 * - The store implementation in `index.ts` wires it together
 *
 * We can add real clarity in the documentation without overwhelming the logic.
 * Keep the code simple and easy to read.  Extra details go here.
 *
 * Start simple. Refactor freely.
 */

export type {{pascalCase stateName}}State = {
  items: string[]
}

export type {{pascalCase stateName}}Actions = {
  /**
   * addItem
   * ---
   * Adds a new item to the collection.
   */
  addItem: (item: string) => void

  /**
   * removeItem
   * ---
   * Removes an item by index.
   */
  removeItem: (index: number) => void

  /**
   * reset
   * ---
   * Resets back to the initial state.
   */
  reset: () => void
}