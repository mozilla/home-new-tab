/**
 * {{pascalCase stateName}} types
 * ---------------------------------------------------------
 * This starts life as a tiny snack counter.
 * NOTE: Replace everything here with your real feature ...
 * (unless of course you're building a snack counter 😉)
 *
 * Add more fields and actions as your feature grows.
 */

export type {{pascalCase stateName}}State = {
  snackName: string   // Name of the current snack
  totalSnacks: number // How many snacks we currently have
}

export type {{pascalCase stateName}}Actions = {
  /**
   * eatSnack
   * ---
   * Eat one snack (silently no-ops if you're out).
   */
  eatSnack: () => void

  /**
   * addSnack
   * ---
   * Defaults to 1; pass an amount if you’re feeling ambitious.
   */
  addSnack: (amount?: number) => void

  /**
   * resetSnacks
   * ---
   * Reset back to the initial snack situation.
   */
  resetSnacks: () => void
}