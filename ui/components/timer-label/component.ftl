# Accessible name for the button that opens the duration editor.
timer-label-edit-focus =
    .aria-label = Edit focus duration
timer-label-edit-break =
    .aria-label = Edit break duration

# Accessible name for the minutes input field inside the editor.
timer-label-input-focus =
    .aria-label = Set focus minutes
timer-label-input-break =
    .aria-label = Set break minutes
