# Timer section heading shown in the widget header
timer-title = Timer

# Phase tab labels — toggle between focus and break sessions
timer-tab-focus = Focus
timer-tab-break = Break

# Control buttons — icon-only buttons use .aria-label for accessible names
timer-button-start =
    .aria-label = Start
timer-button-pause =
    .aria-label = Pause
timer-button-reset =
    .aria-label = Reset
