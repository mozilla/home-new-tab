# Preference toggle row labels
timer-menu-auto-switch = Auto switch phases
timer-menu-auto-start-break = Auto start break

# State label for each preference toggle — shown alongside the row label
timer-menu-preference-on = On
timer-menu-preference-off = Off
